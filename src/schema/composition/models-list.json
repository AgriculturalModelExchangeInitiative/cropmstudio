{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "composition/models",
  "title": "Composition Models List",
  "description": "Schema for the list of models in a composition",
  "type": "object",
  "properties": {
    "models": {
      "type": "array",
      "description": "Array of model references to include in the composition",
      "items": {
        "$ref": "model-reference.json"
      },
      "minItems": 1,
      "uniqueItems": true
    }
  },
  "required": ["models"],
  "additionalProperties": false
}
