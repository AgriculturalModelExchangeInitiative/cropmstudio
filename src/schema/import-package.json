{
  "type": "object",
  "title": "Import Package",
  "description": "Import a package from a local zip file",
  "label": "Import Package",
  "additionalProperties": false,
  "properties": {
    "package": {
      "type": "string",
      "format": "data-url",
      "title": "Package to import",
      "description": "The package must be an archive in zip format"
    }
  },
  "required": ["package"]
}
