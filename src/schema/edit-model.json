{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "edit-model",
  "title": "Edit model",
  "description": "Edit an existing model",
  "label": "Edit Model",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "package": {
      "type": "string",
      "title": "Package",
      "description": "The package to edit the model",
      "enum": [""]
    },
    "model": {
      "type": "string",
      "title": "Model",
      "description": "The model to edit",
      "enum": [""]
    }
  },
  "required": ["package", "model"]
}
