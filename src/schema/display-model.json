{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "display-model",
  "title": "Display Model",
  "description": "Schema for displaying a model workflow",
  "type": "object",
  "properties": {
    "Path": {
      "type": "string",
      "description": "Path to the package directory"
    }
  },
  "required": ["Path"]
}
